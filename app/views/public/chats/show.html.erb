<h1 id="room" data-room="<%= @room.id %>" data-user="<%= current_customer.id %>">
<div class="container-chat">
<%= @customer.name %> さんとのチャット</h1></div>

  <div class="box-chat">
    <div>
    <div class="message">
      <% @chats.each do |chat| %>
        <% if chat.customer_id == current_customer.id %>
          <div class="balloon-color right">
        <!--<figure class="icon-color"><img src="画像のパス" alt="代替えテキスト" >-->
        <!--  <figcaption class="name-color">アイコンの名前</figcaption>-->
        <!--</figure>-->
            <div class="chatting-color">
              <p class="text-color">
                <%= chat.message %>
              </p>
            </div>
          </div>
        <% else %>
          <div class="balloon-color left">
          <!--<figure class="icon-color"><img src="画像のパス" alt="代替えテキスト" >-->
          <!--  <figcaption class="name-color">アイコンの名前</figcaption>-->
          <!--</figure>-->
            <div class="chatting-color">
              <p class="text-color"><%= chat.message %></p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
  </div>

<div class="row">
  <div class="col-md-6 offset-md-3">
  <%= form_with model: @chat, data: {remote: true} do |f| %>
    <%= f.text_field :message, class: "form-control" %>
    <%= f.hidden_field :room_id %>
    <%= f.submit "送信", class: "btn btn-success" %>
  <% end %>
  </div>
</div>

</div>
